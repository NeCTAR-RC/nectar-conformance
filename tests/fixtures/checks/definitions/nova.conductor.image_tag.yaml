---
id: nova.conductor.image_tag
title: Nova conductor image tag is pinned to the approved release
description: >
  The nova-conductor container on the controllers must run the Core Services approved
  kolla image tag.
spec_section: "2.2.1"
severity: error
kind: declarative
selector:
  type: contains_class
  class: nectar::profile::nova::conductor
query:
  type: resource_param
  resource_type: Docker::Run
  resource_title: nova-conductor
  param: image
  extract: 'regex:.*:(?P<value>[^:]+)$'
assertion:
  op: equals
remediation:
  guidance: Pin the approved nova control-plane image tag in the controller nodegroup hiera.
  hiera_key: nectar::profile::nova::common::image_tag
  hint_file: data/nodegroups/<oc-nodegroup>.yaml
