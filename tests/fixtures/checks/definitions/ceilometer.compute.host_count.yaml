---
id: ceilometer.compute.host_count
title: At least the required number of ceilometer compute agent hosts
description: The ceilometer compute agent profile must run on at least the required number of hosts.
spec_section: "2.2.6"
severity: error
kind: declarative
selector:
  type: all
query:
  type: count
  class: nectar::profile::ceilometer::agent_compute
assertion:
  op: count_gte
remediation:
  guidance: Apply the ceilometer compute agent profile to at least the required number of hosts.
  hiera_key: nectar::profile::ceilometer::agent_compute
